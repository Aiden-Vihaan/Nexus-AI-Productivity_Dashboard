# NEXUS Decision Scoring Model

## Purpose

The scoring model evaluates candidate actions using multiple independent productivity signals.

---

# Score

Each candidate receives a normalized score:

```text
0 ≤ score ≤ 1
