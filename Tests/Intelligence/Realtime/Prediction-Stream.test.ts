describe("Prediction Stream", () => {
  it("should react to relevant context changes", async () => {
    // Arrange

    // Act

    // Assert
  });

  it("should produce an updated prediction event", async () => {
    // Arrange

    // Act

    // Assert
  });
});
