describe("Context Stream", () => {
  it("should update context when a context event arrives", async () => {
    // Arrange

    // Act

    // Assert
  });

  it("should emit a derived context update event", async () => {
    // Arrange

    // Act

    // Assert
  });
});
